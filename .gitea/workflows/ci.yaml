name: Keycloak SSH Auth CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: linux_amd64
    steps:
      - uses: actions/checkout@v4

      - name: Verify dependencies
        run: go mod verify

      - name: Build binary
        run: make build

      - name: Build PAM module
        run: make pam

      - name: Run tests
        run: make test

      - name: Run vet
        run: make lint
